#pragma once
#include <set>
#include <map>
using namespace std;
// mot so ham trong set:
// insert s.insert(value) them gia tri vao set
// count s.count(value need to check exist) dem 
// find =set kiem tra xem gia tri co ton tai(1) hay ko(0)
// size s.size cho bt so phan tu cua set
// Erase s.erase(value need to delete)xoa phan tu

// mot so ham trong multiset(=set nhung cac phan tu co the trung nhau)

// mot so ham trong map:
// insert mp.({}{})??
// count(return type value: int);
// find(return type value: iterator)
// ordered(arrange)